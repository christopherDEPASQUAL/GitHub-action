all:
  vars:
    ansible_connection: docker
    ansible_python_interpreter: /usr/local/bin/python3.12
  children:
    webservers:
      hosts:
        ansible-node1:
    appservers:
      hosts:
        ansible-node2:
